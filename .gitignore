# Dependencias
/node_modules
/.pnp
.pnp.*

# Next.js
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# Pruebas
/coverage

# Variables de entorno y credenciales
.env*
# Excepción: la plantilla sin valores sí se versiona — documenta qué variables
# necesita el proyecto sin exponer ninguna.
!.env.example
.vercel

# Archivos de bloqueo temporal de Microsoft Office (se crean al tener un .xlsx abierto)
~$*

# Ejecutable portable compilado — se regenera con Compilar-Portable.bat,
# no debe versionarse (binario grande, siempre derivable del código fuente).
PlaneacionEstrategica2027-2037.exe
launcher-src/portal.zip

# Carpetas temporales que crea Compilar-Portable.bat
/portal-build-portable/
/portal-portable/

# Área de descarte para revisión manual — nunca se versiona.
/Borrar/

# Backups temporales de los Excel fuente
data/**/*.backup*.xlsx

# Evidencia de QA de los diagramas archify (se regenera; no se versiona)
docs/diagramas/*.visual-check.*

# Varios
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
